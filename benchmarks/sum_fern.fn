# Sum benchmark - using recursion (Fern style)
fn sum_to(n: Int, acc: Int) -> Int:
    if n <= 0: acc
    else: sum_to(n - 1, acc + n)

fn main() -> Int:
    let result = sum_to(10000, 0)
    println(result)
    0
