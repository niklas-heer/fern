fn source(ok: Bool) -> Result(Float, Float):
    if ok: Ok(1.25) else: Err(-2.5)
fn choose(ok: Bool) -> Float:
    with
        value <- source(ok)
    do
        value * 2.0
    else
        Err(error) -> error
fn main():
    println(choose(true))
    println(choose(false))
