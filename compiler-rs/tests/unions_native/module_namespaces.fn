import lib.api.{Choice}
fn show(value: Choice) -> Unit:
    match value:
        n: Int -> println(n)
        s: String -> println(s)
fn main():
    let lib = 42
    let value: Choice = Choice(4294967296)
    show(value)
    let f = Choice
    show("fern" |> f)
    println(lib)
