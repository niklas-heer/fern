fn argument(tag: String, value: Int) -> Int:
    println(tag)
    value
fn count(n: Int, value: Int) -> Int:
    if n == 0: value
    else: count(n: argument("first", n / 0), value: argument("later", value))
fn main():
    defer println("cleanup")
    println(count(n: 1, value: 2))
