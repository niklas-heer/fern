type User derive(Json):
    id: Int
    name: String
fn encode() -> Result(String, json.Error): json.encode(User(1, "fern"))
pub newtype UserId derive(Json) = Identity(Int)
newtype Box(a) derive(Json) = Packed(a)
